# Overnight continuation — September 13, 2026

## PAUSED at the user's request — September 13

Development is paused. Do not start more work until the user resumes. No training, export, acoustic or serial test is active. The only continuing test is the previously started M6 24-hour simulator soak, PID42401, `work/m6/soak-24h-retry-20260913/results.json`; at this checkpoint it had2204 seconds andzero failures. The user was told this soak would continue. The board is quiet, wake activation suppressed, with the default-disarmed MultiNet6 experiment flashed. The latest compiled microWakeWord runtime-guard build is NOT flashed and embeds the known-failing public V1 model. Do not mistake that build for an accepted candidate.

All three local training feasibility candidates are rejected and remain private research artifacts. V1 direct streaming accepted20/20 negatives. V2 detected14/20 positives and falsely accepted13/20 negatives. V3 detected20/20 positives but accepted20/20 negatives in the original diagnostic set; more decisively, the reserved Daniel/Tessa set detected8/8 positives but falsely accepted160/160 negatives (`work/wake-research/custom-v3-heldout-corpus.json`). V3's float streaming and nonstreaming paths both also falsely accept Hey Mary (`conversion-check-v3.json`), so the failure is present before quantization. None of these trained candidates was flashed, and wake/M8 acceptance is NOT complete.

Resume by investigating the discrepancy between high end-window validation scores and continuous false activations: compare actual full-utterance feature windows with saved negative training examples, labels, augmentation and temporal coverage before spending more training runs or flashing. The first feature generator's178-frame versus214-frame context mismatch was corrected in V2/V3 but did not resolve the failures. Dataset generation, model training, calibration/export and direct reference scripts are preserved under `work/wake-research`. The trainer is OHF commit4665173cd35f1cff9a61e06fc427f124766c488e with one local calibration-only change to drop an incomplete final stride. Python environment is `work/wake-research/venv` (Python3.12, TensorFlow2.18.1, pymicro-features2.0.2); generated audio uses local macOS voices, no human recording or external training service. V3 training includes Samantha/Karen/Moira; only Daniel/Tessa in the new168-case set are reserved voices. Future acceptance needs fresh representative human/noisy/ambient data and lifecycle tests, not just these synthetic examples.

Verified source improvements: no stock HiESP fallback; wake autostart defaults off until acceptance; micro runtime validates model quantization/output shape, frees allocations on initialization failure and uses a stride-aware two-second settling period. The full audible micro build passed (`work/board/build-wake-runtime-guards.log`). `body-audio-smoke.py` now explicitly confirms diagnostic wake arming, measures this-run wake/interruption deltas and suppresses activation afterward; Python compilation passed, but no live voice trial of that revised helper was performed. Hardware default-disarmed boot check passed withzero captured/uploaded samples andzero AFE faults; a later15-second observation measured15,748 processed samples/s. Speaker volume remains95/100, user-approved for the8-ohm speaker. Computer volume unchanged.

## Current wake investigation — September 13, after USB reconnect

Latest update: physical default-disarmed build was flashed and its reboot check passed (`work/board/wake-default-disarmed-check.json`); subsequent local processing measured approximately15,748 samples/s. No serial test is currently running. The newer microWakeWord runtime guard build passed (`work/board/build-wake-runtime-guards.log`) but was NOT flashed; it embeds the old V1 evaluation asset. Do not flash that build as an accepted model.

Local model training v1 and v2 failed direct continuous tests. Training v3 has finished and conversion is running (`work/wake-research/export-training-v3.log`). V3 adds Samantha/Karen/Moira to training, so those voices in the original40-case corpus are no longer held out. A new168-case set in `work/wake-research/heldout-natural/manifest.json` reserves Daniel/Tessa. Next run `heldout-reference-v3.py` and assess results before any candidate flash. All scripts/environments/weights remain under `work/wake-research`; no training service or user recording was used. Firmware source now has no HiESP fallback; micro runtime validates quantization, cleans up failed initialization and uses a stride-aware two-second settling period. Source changes compiled, but newest changes are not on the board yet.

The earlier active-test entries below are historical. USB serial access is restored. No physical serial test is currently running; automatic voice activation is suppressed after failed wake evaluations. Docker and the isolated board backend are healthy again after disk exhaustion. The original M6 24-hour soak failed at approximately 20.4 hours with ENOSPC and is not accepted. Its replacement is running: PID42401, `work/m6/soak-24h-retry-20260913/results.json`, started September13; do not stop it.

Current flashed image is the MultiNet6 segmented competing-phrase experiment, recorded in `work/board/afe-audible-flash.json`. It missed all5 positives in the10-case trial. Dedicated microWakeWord V3 scored16/20 positives with10/20 negative false activations; V1 scored18/20 with9/20 negative false activations. None is accepted. See `hey-marvin-wake.md`. A local Python reference evaluation is being prepared under `work/wake-research/venv` to distinguish model limitations from firmware/frontend errors. No training job or external model request has been started.

The user authorized continuing M8–M11 while away until 7 AM America/Los_Angeles. A thread heartbeat, `marvin-overnight-implementation`, runs every 30 minutes, now extended through15:30 local to collect the long-test results. Keep speaker output disabled until the user actually returns. Do not ask for intervention while they are asleep. Do not declare the whole system complete: original acceptance gates in `implementation-plan.md` remain unchanged.

## Active tests: do not disturb

- Physical ESP32-S3 `/dev/cu.usbmodem1101`: eight-hour silent local AFE/runtime soak started approximately 01:40 local, Python PID can be found by `ps` matching `board-runtime-soak.py`. Tool session 55151. Numeric progress: `work/board/runtime-soak-1789288828218.json`; final stdout `work/board/runtime-native-8h.log`. It should finish approximately 09:40 local. **Do not open serial, reset, flash, or start another board script until this process finishes.** All observations so far are idle/silent with no AFE faults. This checks local processing, not wake accuracy or AEC quality.
- Physical gateway idle observation: tool session 86949, `scripts/board-idle-soak.ts --seconds 3600`, started approximately 01:40 local. Progress is the newest `work/board/idle-*/results.json`; stdout `work/board/idle-native-60m.log`. At eight minutes it had 92 samples/zero failures. **Do not restart the isolated backend or intentionally disconnect the board during this test.** It is application-layer evidence, not packet capture.
- M6 simulator 24-hour soak: PID 36209, tool session 4764, `work/m6/soak-24h/results.json`. Started September 12 at 21:55 UTC; at 10.8 hours had zero failures, no provider/audio activity, peak stabilized RSS growth 9.145%. Do not stop. Finish approximately 14:55 local September13.

## Verified changes this iteration

- Protocol 1.2 supports native 16 kHz mono s16le board upload. The server converts to 24 kHz with a streaming 63-tap FIR. Prior protocol 1.0/1.1 clients remain 24 kHz. New firmware requires an explicit 16 kHz input acknowledgement. Speaker wire format remains 24 kHz.
- Physical silent 30-second upload **passed**:480,000 samples sent; no queue overflow, reset or firmware fault; clean stop; speaker amplifier disabled. Evidence `tests/acceptance/results/M08/silent-native-uplink-30s.json`. This is the first sustained capture success after earlier24 kHz queue-overflow failures. No acoustic or latency acceptance claimed.
- Full check after native change: 134 tests/typecheck/build passed. Full check after Deepgram integration: 137 tests/typecheck/build passed. Two additional focused Deepgram failure tests then passed, and final typecheck/diff check passed. Run full check again after further changes.
- New `packages/runtime/src/deepgram-voice.ts`, optional `VOICE_PROVIDER=deepgram`, explicit key/STT/TTS models. Uses canonical configured text model for reasoning/tools and separate streaming STT/TTS sockets. Tests 20 turns, single-use utterances, reconnect generation, duplicate transcripts, abort, malformed recognition and invalid PCM. No Deepgram or Anthropic live credentials available. See `m10-provider-portability.md`. Not live parity acceptance.
- `firmware/components/marvin_update/esp_writer.c` adds ESP-IDF inactive-slot callbacks, durable pending metadata and idempotent health confirmation/sequence commit. Builds for ESP32-S3. Host platform fixture tests persistence failure/recovery and rollback boundaries ten times each; manifest/transfer sanitizer suites pass. Current profile does NOT enable rollback, and no download/install call site exists. It is not a working OTA feature yet. See `m11-firmware-updates.md`.

## Concrete next work

1. Review the new second speech adapter for bounded cancellation, authentication, session isolation and rate/error handling. Extend meaningful failure/parity fixtures where needed; do not infer live parity without credentials.
2. Finish OTA integration in code while the board soaks: dedicated public release-key provisioning (separate from enrollment keys), authenticated same-origin HTTPS release download without redirects, task quiescence before flash operations, bounded boot-health confirmation, rollback-enabled profile, and operator-controlled release serving. Preserve existing owner/factory offsets and never burn eFuses. Prepare tests/builds, but **do not flash until the active soak ends**. Use an isolated build output/profile if changing generated configuration so recorded test firmware remains identifiable.
3. Save sanitized completed soak results to acceptance folders, retaining unsuccessful results and scope limitations. Avoid repeated heavy regression runs without new changes.
4. Prepare a precise user-assisted checklist for actual wake/quiet speech/AEC/barge-in, Web Bluetooth/alternate Wi-Fi, and physical OTA interruptions. The board is standalone; no motor/display/sensor acceptance can be claimed. Entire third-party identity, second-provider live parity, independent installation/usability and seven-day beta remain external gates.

## Environment and safe operations

Project `/Users/stefanopedemonte/Projects/Marvin_software`; no AGENTS, no agent delegation authorized. Many intentional uncommitted files; do not clean/reset them. No git commit/push performed.

ESP-IDF 5.4.2 is in `work/esp-idf`; use `work/idf-tools/python_env/idf5.4_py3.13_env/bin/python` without resolving its symlink. Board ESP32-S3 revision 0.2, 16 MB flash, 8 MB PSRAM, MAC 28:84:85:b2:ab:10. Verified original backup is `work/board/backup-20260912T221258Z`. Factory owner image is in `work/board/factory-owner-288485b2ab10`; do not read/print its secrets. Current flashed profile is silent AFE/native 16 kHz, recorded in `work/board/flash-afe-native.log`. The later build includes unconnected OTA platform code but has **not** been flashed.

Isolated backend container `marvin-board-backend-marvin-1`, origin `https://192.168.1.5:8443`, CA `work/board/board-backend-root.crt`, private configuration `work/board/backend.env` and `backend-login.json`. It currently runs the native 16 kHz conversion/diagnostics, not the later optional Deepgram integration. Do not restart during idle observation. Do not print environment values, passwords, bearer credentials, factory secrets or private release/enrollment keys. Do not touch unrelated containers.

No speaker playback (`afplay`, audible `say`, tones) while sleeping. `CONFIG_MARVIN_SILENT_TEST=y` physically forces PA off; WakeNet activation is suppressed. No audible test has been run during this overnight work.

## 02:23 continuation

- The physical soak and gateway idle observer remain undisturbed; at02:19 both showed no failures (approximately39 minutes elapsed). No speaker output or new board access.
- Fixed a reproduced Deepgram race: repeated clear/reconnect could let a superseded connection failure close the newest session. A20-clear regression now passes. Fixed `/api/config` so the portal advertises configured Deepgram voice; added an API regression.
- Added optional verified firmware rollout serving: `packages/operations/src/firmware-release.ts`, `FIRMWARE_ROLLOUT_FILE`, and authenticated versioned device manifest/image GET endpoints. Explicit device allowlist only; no rollout is configured or published. Startup validates public P-256 key/signature/image digest and loads immutable bounded bytes. Tests cover tampering, size, immutable bytes, targeting and unlink revocation. See the new rollout section in `m11-firmware-updates.md`.
- Next OTA implementation can use GET `/api/device/firmware` for an offer, then its versioned manifest/image paths with the existing device bearer credential. Firmware must independently verify and enforce its factory trust/committed sequence. HTTP consumer, quiescence, factory release key and boot-health call sites are still absent. Do not flash during the physical soak.
- Full regression log for this continuation: `work/board/check-overnight-rollout.log` (completed:144 tests, typecheck and production build passed). No deployment was restarted.

## 02:53 continuation

- The60-minute application-layer idle check completed:719 samples, zero failures, stable connection, no new provider session or audio traffic. Evidence copied to `tests/acceptance/results/M08/idle-application-60m.json`. This does not replace packet capture. Eight-hour physical soak remains running with no fatal diagnostics; do not access serial or flash it.
- Added `marvin_update_download` in the update component. Fixed same-origin HTTPS paths, current device bearer format, provisioned/public CA, redirects disabled, exact lengths, independent signed manifest/version verification, bounded chunks, cancellation and120-second deadline. It invokes the existing transfer engine; no application call site or automatic install exists.
- ESP32-S3 build passed (`work/board/build-update-download.log`). Update sanitizer suite passed including80 new HTTP-fixture fault trials (`work/board/update-download-host.log`). No failed download selected an image. No deployment change or hardware update was performed.
- Next: factory release public-key provisioning/loading, exclusive update admission, safe audio/AFE task quiescence, rollback-enabled isolated profile and boot health invocation. `factory.py` currently only creates new identity/SRP credentials; do not recreate the existing board identity. Add optional release public trust for new factory creation and a careful existing-factory augmentation path if needed. Keep enrollment signing and firmware signing trust separate.

## 03:23 continuation

- The silent physical soak remains active (approximately101 minutes, zero fatal diagnostics, all observations idle/silent). No serial access, deployment restart, audio output or board update.
- Added optional public firmware trust to new identity creation (`factory.py --release-public-key`) and a separate identity-preserving CSV tool (`release-trust.py`). The latter refuses overwrite, key replacement, enrollment-key reuse, duplicate fields and private/non-P-256 keys. Only temporary fixture identities were used; the real factory data is unchanged.
- Added `marvin_update_factory_trust` to load/validate the public `release_pub` blob and committed sequence, bound to a trusted compiled layout/slot size. Host loader and temporary factory tests pass. Full update sanitizer suite and ESP32-S3 build pass (`work/board/update-factory-host.log`, `build-update-factory.log`).
- Next: exclusive update admission, safe audio/AFE quiescence, rollback-enabled isolated profile, bounded boot-health invocation and application update command. Existing helper APIs are in `marvin_update_factory.h`, `marvin_update_download.h`, and `marvin_update_esp.h`. Do not flash or reopen serial while the8-hour physical soak is running. No release rollout or real factory key has been configured.

## 03:53 continuation

- Physical silent soak and M6 simulator remain healthy and undisturbed. Approximately131 minutes of physical runtime; no fatal diagnostics. M6 remains below10% peak stabilized RSS growth with zero failures.
- Added one-way `marvin_device_link_quiesce` and `marvin_body_quiesce` / `marvin_audio_quiesce` for OTA preparation. Transport disconnects and parks with a10-second deadline; reconnect backoff can be interrupted. Audio prevents new capture, waits for all capture/feed/playback task acknowledgements, clears playback state and mutes/disables I2S DMA. It does not resume except by reboot. Failed shutdown must prevent installation. No runtime call site yet invokes these APIs.
- Build logs: `work/board/build-quiesce.log` (AFE), `work/board/build-owner-quiesce.log` (fallback owner). Both final builds completed successfully; the final edits made lock waiting bounded and handle missing/partially initialized audio conservatively. Physical quiescence is NOT verified and cannot be tested until the active board soak finishes.
- Next implementation: optional signed-update task/command using the current owner snapshot, loaded factory release key and fixed HTTPS version paths; separate rollback-enabled build profile; bounded first-boot health confirmation. Invoke transport shutdown before audio shutdown and abort/reboot safely on any failure. Do not enable a real rollout or modify factory/board state during the soak.

## 04:24 continuation

- Added optional `ota_runtime.c`: single update admission, current-owner preflight, transport/audio quiescence, signed download, cancellation/ownership-expiry checks, and reboot to selected/prior image. `u<decimal sequence>\n` is the local test request, `x` cancels. Normal profiles keep activation disabled.
- Added isolated `build-waveshare-ota.sh` / `waveshare-ota.defaults`: silent AFE, signed OTA and rollback-enabled bootloader. No physical install. The runtime starts early enough to supervise trial boot health for90 seconds, requires five stable seconds of presence/audio/AFE progress, confirms signed pending state, and requests rollback on failed trial health. Valid images can reconcile metadata without being invalidated for an outage.
- Fixed a boot-confirmation correctness gap: missing/wrong pending metadata can no longer return success for a PENDING_VERIFY image. Updated NVS fixture covers this; full update sanitizer suite passes (`work/board/ota-runtime-host.log`).
- Isolated build log `work/board/build-ota-runtime.log`; final build passed after the console cancellation adjustment. Next review/test the worker and initial installation path before enabling it. `board-flash.py` does not yet accept the ota profile. Initial rollback trials must retain a real usable fallback slot and preserve ownership; do not improvise a destructive install.
- The real board remains on its earlier silent firmware with the8-hour soak in progress. No serial access, signer creation, factory modification, backend rollout or audible test.

- User-assisted checklist is in `docs/user-assisted-acceptance.md`; respect the physical soak through approximately09:40 even if the user returns at7, unless they explicitly reprioritize it.

## 04:53 continuation

- Added host tests of the real coordinator through mocked platform/task boundaries:90 update admission/cancel/shutdown/download trials, stable five-second trial health, four90-second health failure modes, and valid-image metadata reconciliation. Full update sanitizer suite passes in `work/board/update-coordinator-host.log`.
- Added a fallback prerequisite to `marvin_update_esp_writer`: the running application must have valid OTA metadata and state VALID. PENDING_VERIFY or missing/unconfirmed state cannot become an assumed fallback. Platform fixture verifies rejection. This makes initial OTA bootstrap explicitly necessary; do not just add the ota profile to the existing flash helper, which resets OTA metadata and overwrites ota0.
- Build `work/board/build-ota-fallback-guard.log` is the isolated OTA profile with the new guard. Final build passed. No physical activity, rollout or factory change.
- Next: plan/test a non-destructive initial bootstrap that retains a usable image and valid metadata using the pinned ESP-IDF bootloader semantics. Hardware trials remain blocked on the active soak completing. Independent cleanup/review and missing browser/second-provider fixture coverage can continue without resetting the board.

## 05:23 continuation

- Reviewed initial bootstrap against pinned ESP-IDF sources. Existing flasher resetting OTA metadata is unsuitable; candidate VALID bypasses initial trial health, and candidate NEW without signed pending metadata is correctly refused. No installation shortcut was added. See `docs/ota-bootstrap-review.md` for the concrete prerequisites/design options.
- Added offline-only `firmware/tools/ota_metadata.py` and a temporary fixture comparing its CRCs directly against the compiled ESP-IDF ROM CRC implementation. CRC, slot/state and corruption cases pass. It has no CLI writer/serial access and did not generate a board image.
- Physical soak remains silent/healthy (approximately219 minutes at05:19); M6 remains at9.145% stabilized peak RSS growth and zero failures. No hardware, signer, rollout or factory changes.
- Next independently useful work: audit/test initial bootstrap confirmation design or improve remaining provider/browser integration coverage. Do not enable ota in the flasher before a preserving, health-checked bootstrap exists. Physical tests remain deferred until after the8-hour soak (approximately09:40).

## 05:55 continuation

- Added20 fresh authenticated web-voice sessions through the actual Deepgram adapter against local WebSocket fixtures. PCM upload/output, provider retirement and canonical history all pass. No live provider/board/browser audio used.
- Fixed uncaught provider `clear()` exceptions in the mute handler; voice now closes safely and text remains available.
- Fixed mute races across asynchronous context retrieval/database admission. Input generation fences superseded transcripts; a late-created turn is cancelled after admission. New regressions pass, including the database case that initially reproduced a completed turn after mute.
- Full software check is in `work/board/check-voice-lifecycle.log`; completed with148 tests, typecheck and build passing. No backend restart, serial access or sound. Next audit the analogous device-body voice lifecycle for ownership/close races across awaited context/start operations; its state checks may need the same treatment.

## 06:23 continuation

- Audited/fixed body-voice async lifecycle: pending context/admission work is fenced by exact session identity and interrupt generation; late-admitted work is cancelled. Old callback failures cannot stop a replacement session on the same device. Provider close exceptions do not prevent local session cleanup.
- Four authenticated simulated-device regressions cover stop/restart and interrupt during both context retrieval and database admission; no late audio, no resurrected turn, and no replacement-session closure. Existing normal16/24 kHz routed-body tests remain included.
- Full check log `work/board/check-body-lifecycle.log`; completed:152 tests, typecheck and build passed. No real backend restart, serial access or sound. Board soak remained healthy at~281 minutes; it still must reach8 hours.
- On user return, keep sound disabled until coordinated. The user-assisted checklist is in `docs/user-assisted-acceptance.md`; provider credentials/browser review can proceed before the board soak ends. Initial OTA bootstrap and physical audio remain unaccepted.

## 06:52 continuation

- Consolidated `delivery-status.md` to remove contradictory current M8–M11 claims and clearly separate verified implementation from unmet release acceptance. Latest software check remains152 tests/typecheck/build passed; no unnecessary rerun.
- Physical soak passed5.2 hours of observation so far with no fatal diagnostics, but is incomplete until~09:40. M6 remains running with zero failures and9.145% peak stabilized RSS growth until~14:55.
- Extended the existing heartbeat through15:30 local September13 so result collection is not lost when the overnight window ends. Quiet unless meaningful change/action. Sound remains disabled until the user actually returns. No physical/deployment changes.
- If the user is back before09:40, coordinate browser/keyboard/screen-reader checks and any second-provider credentials first. Do not interrupt the8-hour board run unless they explicitly reprioritize it. Initial OTA bootstrap remains a concrete independent design/implementation task, and physical audio/OTA trials remain unaccepted.

## 07:29 continuation

- Implemented signed initial-bootstrap capsule support: optional public `boot_manifest` in factory, normal release-key signature, initial sequence only, running PENDING_VERIFY, no existing pending update, usable ESP-IDF rollback image, exact running-image digest, then normal health/sequence confirmation. Capsule cannot replay after committed sequence becomes nonzero.
- Factory tools accept `--bootstrap-manifest`, validate signature/board/layout/minimum prior version and preserve existing identity. Only ephemeral fixtures were used. No real signer, capsule, factory or board changes.
- Seventy bootstrap fault trials and the full update sanitizer suite pass (`work/board/bootstrap-host.log`). Factory capsule tamper tests pass. Isolated OTA build passes (`work/board/build-signed-bootstrap.log`). Fixed coordinator handling when image confirmation succeeds but later metadata commit continues failing: keep the already-VALID image and reconcile later, rather than invalidating it.
- Remaining initial-install task is a preserving USB staging planner: fresh verified board snapshot after soak, current image/layout/factory/model validation, inactive-slot candidate, preserved prior usable image/ownership, and NEW candidate plus VALID fallback metadata. Legacy flasher still must not be used for ota. No serial access while the8-hour soak remains running.

## Attended physical audio tests, September 13 morning

- User returned, set computer volume3/16, placed standalone board nearby and explicitly requested physical tests. Paused the overnight heartbeat to avoid conflicting hardware work. M6 simulator soak is untouched.
- Interrupted physical silent soak at~6.1hours; original report records interruption and remains not passed.
- Added isolated `afe-audible` bench build, amplifier enabled and independent codec volume50 (approximately−25dB mapping), signed OTA disabled. Flashed with matching verified backup and existing factory identity, preserving owner NVS.
- Rebuilt/restarted the isolated LAN backend with latest tested voice lifecycle fixes. Quiet synthetic computer speech is correctly transcribed through the physical board. Initial reply playback fails; investigating link failure diagnostics. No acoustic gate passed yet.

- User confirmed hearing brief speech from the board. Full software check after server pacing:152 tests, typecheck and build pass (`work/board/check-physical-pacing.log`). Short paced physical reply passed (39,200 playback samples at16k), but initial longer USB-interrupt attempts still overflowed. Current flashed audio build enables the component-supported separate WebSocket TX lock, pins transport tocore0, and bounds receive batches; testing that build now. Keep failed evidence. No acoustic latency or sustained-duplex acceptance yet.

- Subsequent user feedback: full short sentence was audible but crackled; count tests produced only a noisy portion ofone (they deliberately requested interruption, and also had transport faults). Do not mark acoustic quality accepted.
- Timing isolated~195ms receive work for80ms PCM. Raw24k playback queue now separates queue insertion from resampling/hardware lock; measured insertion fell to471µs. Queue duration remains8seconds, now384KB inPSRAM. Removed the extra10ms playback delay and added60ms normal DMA drain before PA mute.
- Receive still overflowed while microphone sends blocked the shared loop. Latest build separates upload into a lifecycle-locked task, keeps receive/control independent, and parks upload during OTA quiescence. Physical verification is next. Serial logs keep numeric timing; no room PCM files.

- The optimized FIR uses direct bounded float rounding and per-fileO2; sanitizer frequency/chunk tests pass. Body playback now consumes24k PCM from a384KB/8s PSRAM queue, resamples only inside the playback task, and holds a separate hardware lock. Pending flushes yield playback before the next chunk. PA waits60ms for normal DMA drain, explicit flush mutes immediately.
- WebSocket ingress is bounded at32 frames; client lifetime is protected while the separate upload task is writing. Firmware trace logs include receive, conversion, write and flush durations.
- CPU placement: microphone AFE feed remains core0/p6; AFE inference, capture fetch, WebSocket reader, receive/control, upload and playback are core1/p5. Short sentence passed25s capture +2.4s playback with no faults after moving most work; longer count exposed receive starvation oncore0, so receive/control was also moved. Latest build additionally sets configurable microphone gain30dB (codec default) because low-volume computer prompts were inconsistently transcribed. Speaker stays50.
- Latest flashed artifact and hashes are in `work/board/afe-audible-flash.json`; flash log `work/board/flash-afe-audible-mic-gain.log`, build `work/board/build-afe-audible-mic-gain.log`. A full count test without intentional interruption is running now. User confirmed earlier brief board speech and later reported crackling on a full sentence; a newer listening-quality question is pending. Do not conflate digital playback success with acoustic acceptance.

- Later timing: arithmetic optimization and real-time priorities brought10ms playback conversion below~2.5ms and writes near11.5ms. Network stays connected, but dual-mic AFE hits DMA ingress overflow (`ESP_ERR_INVALID_STATE`259). Feed frames are1024/64ms; during playback successive measured feed calls average~63ms, leaving inadequate scheduling headroom. No acceptance gate relaxed.
- Testing `MR` (primary mic + actual playback reference), `AFE_TYPE_FD`, `AFE_MODE_LOW_COST`. Espressif benchmark supports this lower-compute full-duplex baseline: https://docs.espressif.com/projects/esp-sr/en/latest/esp32s3/benchmark/README.html . Physical second mic remains sampled by the HAL but is not used in this voice pipeline. Added dynamic feed-channel packing and while-loop aggregation for supported chunk sizes.
- Current priorities: AFE feed core0/p6; AFE inference and capture core1/p6; playback core1/p7; socket/receive/upload core1/p5. Ingress32 frames now uses bounded100ms producer backpressure. Latest build/flash logs: `build-afe-audible-single-mic-fd.log` / `flash-afe-audible-single-mic-fd.log`. Full-count test running; still no sustained-duplex acceptance.

- Latest single-mic FD firmware test correctly transcribed the count prompt, showed no link/audio fault and AFE feed max29ms, but voice closed after~5s with only0.46s of submitted playback. Investigating provider cancellation rather than marking hardware accepted.
- Fixed OpenAI response cancellation: immediate remote-created/done tracking independent of paced consumption; at-most-once cancel including abort before response.created; abort check between yielded PCM chunks. Added three real-SDK local TLS regressions. Full check passes155 tests/typecheck/build (`work/board/check-voice-cancel.log`). Server now logs allowlisted provider error codes only. No blanket ignored errors. Docker rebuilt and isolated physical backend restarted; full count without intentional interruption in progress.

- After cancellation fix, full count passes:25.05s capture,13.4s driver playback, canonical reply1–10 completed, no firmware/link/provider diagnostic. AFE feed max28.4ms, conversion max7.1ms, write max12ms, zero capture discards. Evidence `tests/acceptance/results/M08/attended-complete-count.json`. Latest count listening-quality feedback requested; not yet accepted acoustically.

- Intentional USB interrupt trial stops with1280 samples/80ms submitted after request; canonical turn cancelled, no provider diagnostic or active-session link/audio fault. AFE readError259 appears before listening (startup buffer overrun), so this is not clean startup or acoustic acceptance. Evidence `tests/acceptance/results/M08/attended-usb-interrupt.json`. Board voice is stopped. Latest full-count subjective feedback is still pending.

- User-requested full-count replay: user feedback “Perfect”. Test submitted214,400 playback samples (13.4s), captured401,280 samples, zero audio/link faults, AFE readError0, no intentional interruption. Subjective quality confirmed for this trial. Evidence: `tests/acceptance/results/M08/attended-count-user-confirmed.json`. Voice stopped normally; broader M8–M11 gates remain open.

- User requested substantially more speaker volume after confirming quiet count quality. Audible profile rebuilt/flashed at codec70 (formerly50), owner identity preserved. Build/flash logs `work/board/build-afe-audible-volume70.log` and `flash-afe-audible-volume70.log`; current artifact hashes `afe-audible-flash.json`. First replay lost transport before playback (linkFault1, upload write~523ms); no AFE fault. Retry in progress.

- Volume70 retry passes:25.08s capture,15.3s submitted playback, no link/audio fault, AFE readError0. Voice stopped normally. Evidence `tests/acceptance/results/M08/attended-count-volume70.json`; subjective level/quality feedback pending.

- User confirmed volume70 replay: “Perfect”. Recorded subjective quality confirmation in `tests/acceptance/results/M08/attended-count-volume70.json`. Current speaker setting70 remains installed. This confirms the attended count only, not the full acoustic corpus or milestone completion.

## Robust voice/wake work after user request

- User asked to finish robust voice/wake. Implemented local300ms chronological pre-roll (new `audio_preroll.c/h`) and a bounded5.12s PSRAM capture queue. Capture begins on accepted voice_start; voice_ready enables upload without discarding speech spoken during provider handshake. Host ASAN/UBSAN tests cover wrap, chronological drain, partial chunks and erasure. No audio persisted.
- New `--wake` smoke mode plays stimulus before listening and never sends USB voice_start. It waits for online status and3s of AFE processing. Uses `work/board/wake-question.aiff` synthetic “Hi ESP” followed immediately by request.
- First attempt had codec0x18 probe failure, correctly disabled voice. Second boot processed AFE but did not detect the earlier synthetic phrase; user physically restarted board during that trial, causing expected serial disconnect. Added bounded5-attempt I2C bus-reset/probe recovery. Current flashed build `build-wake-startup-recovery.log`, flash `flash-wake-startup-recovery.log`, speaker70, ownership preserved. Acoustic wake retry running. No wake acceptance yet.
- Espressif current custom-wake docs point to trained-model request/service; installed model remains Hi,ESP. Source https://github.com/espressif/esp-sr/issues/88 . No external submission made.

- Acoustic wake retry passed: one detected Hi,ESP wake, USB start never sent,4800 pre-roll samples retained,26.73s capture,2.1s playback, no faults. Transcript preserved the request structure but STT rendered “wake” as “wait”. No corpus acceptance yet.
- User replaced speaker with tiny1W **8Ω**, connected to same amplifier output; old subjective/acoustic results apply to old speaker. User requested louder firmware. Codec default curve maps70→−15dB,80→−10dB. Building default80 for a short attended test; no maximum-power claim.
- Added local console +/-5 volume adjustment serialized with playback (does not unmute). Added local AFE speech-onset interrupt experiment and64ms VAD settings; compiled but not physically verified yet. Repeated wake during active voice now requests interruption. Current build in progress `build-volume80-local-interrupt.log`. Last flashed firmware is still startup-recovery/preroll at70 until new flash succeeds.
- Current flashed firmware is now volume80 + local AFE onset interrupt + pre-roll, build/flash `build-volume80-local-interrupt.log` / `flash-volume80-local-interrupt.log`. Short wake-only test passes withone wake,300ms pre-roll,2.1s playback, zero local interrupts/no hardware faults. User feedback on new8Ω/1W speaker level pending. Next acoustic barge test runs wake-count followed by computer-spoken “Stop counting…blue sky” after1s driver playback; never sends USB interrupt.
- Acoustic barge test failed before the intended second stimulus: localInterrupts1, playback0.4s, no second fixture played. Volume90 short test likewise self-interrupted. Its older smoke predicate said passed based on transport; that does NOT establish completed playback. Updated predicate rejects unsolicited local interruption. User said80 remains too quiet but clear; runtime90 accepted, quality still pending.
- Testing echo rejection: explicitly keep processed (not raw-after-wake) channel, disable AGC, use VAD_MODE_3/min96ms +64ms continuous speech before local flush. Ignore repeated wake detections while capture is active. Build in progress `build-barge-echo-rejection.log`. No physical AEC/barge gate passed.

- Echo-rejection build flashed (`flash-barge-echo-rejection.log`), default volume80/runtime adjustable. At runtime90 full wake-count completes1–10 with no spurious local interruption; subsequent acoustic barge test missed the quiet computer interruption (no local interrupt/transcript), so barge remains open. User reports90 still too quiet but clear. Runtime95 short wake test passes2.3s playback/no faults/no local interrupt; subjective95 feedback pending. Last test auto-stopped voice. Silent sibling profile compile in progress to catch regressions.

- User accepted95 as loud enough and clear on small speaker. Default95 build/flash succeeds (`build-volume95-startup.log`, `flash-volume95-startup.log`). Silent sibling build succeeds via `sh firmware/tools/build-waveshare-afe.sh`; no silent firmware flashed. Runtime +/- commands available. Natural-voice helper added (`--manual`,120s wake wait, no computer stimulus); awaiting user availability while passive manual test is running. No complete robust-voice acceptance claimed.

- Passive manual trial ended at its120s wall deadline shortly after late wake; no complete intended count/interruption exchange recorded. Only5.67s capture,0.52s playback,1local interrupt. Marked failed/indeterminate, not barge acceptance. Corrected helper to allow full25s after late manual activation and accurately label human scope. Serial closed and voice stopped. Human-test availability question remains unanswered. Default95 is physically flashed and user-approved on8Ω1W speaker.

- User confirmed natural-voice wake/interruption: “Worked”. Backend independently confirms count turn cancelled after5 and following request “Stop counting and say blue sky” completed with “Blue sky.” Scoped evidence `tests/acceptance/results/M08/natural-wake-interrupt-user-confirmed.json`. Natural interaction now demonstrated; instrumented acoustic timing, corpus accuracy and sustained reliability remain unaccepted. No proof here distinguishes local from provider VAD cancellation.


## Exact Hey Marvin work — September 13, current checkpoint

- Latest user requirement is exact **Hey Marvin**. Hi ESP is historical evidence only. Local experimental MultiNet detection replaces the stock wake model in the AFE profile, disarmed while capture is active. No idle microphone upload or open provider session is used for detection.
- MultiNet5 generated acoustic trials failed: threshold0.90 yielded0/20 positives; threshold0.50 with a duplicate application score cutoff removed yielded1/20 positives and0/20 negative activations. Evidence `work/board/wake-corpus-1789321830550.json`. This does not meet wake acceptance. All40 cases ran, feed faults0, uploaded samples0.
- New `firmware/tools/wake-corpus.py` uses generated local AIFF clips, bench observation mode (`w` disables activation, `W` restores it), and refuses playback without explicit observation confirmation. It records detections and numeric model/AFE diagnostics without recording room PCM. `--computer-gain` can reduce stimulus; system volume was observed at38%, not changed by this work. Speaker remains95.
- MultiNet6 English graph `HEY MARVIN` fits unchanged application/model partitions. Current generated audible config selects MN6; silent generated config remains MN5 and needs regeneration/rebuild before claiming matching-profile validation. Source supports both. Model is3,788,231 bytes. Current successful build `work/board/build-hey-marvin-mn6-levels-retry.log`, flash `work/board/flash-hey-marvin-mn6.log`, authoritative hashes in `work/board/afe-audible-flash.json`. No layout/eFuse/security changes; same current-owner factory and original verified backup used.
- **Physical blocker:** flash verified successfully, but the firmware/USB serial subsequently returns no data. Corpus `work/board/wake-corpus-1789322020746.json` contains zero cases and empty diagnostics, failed observation readiness; no samples played. Ordinary hard reset and esptool standard and USB-specific connection attempts both returned no serial data. Need a physical USB-C unplug/reconnect, then inspect boot diagnostics and run MN6 trial. Do not claim model startup or acoustic success. All serial helpers closed; no hardware test is running.
- Added interval microphone peak and model name to numeric status. Corrected observation-status flag to reflect runtime suppression. Bench recognizer's native threshold is0.50, valid command1 only; no duplicate returned-score threshold. This is experimental and must meet corpus/false-wake/latency/soak gates before completion.
- Host disk pressure caused two build failures. Removed only project download caches (`work/npm-cache/_cacache`, `work/idf-tools/dist`) and old reproducible `esp-idf` intermediate object directories in the five other build trees; retained binaries, logs, source, credentials, board backups and evidence. Retry with CMAKE_BUILD_PARALLEL_LEVEL=2 succeeded. Those older build trees will need regeneration before future builds. Disk remained near capacity (~1.6GB free).
- M6 24h simulator PID36209 remains running; its independent acceptance is not complete. Overnight hardware automation remains paused to avoid contention.
